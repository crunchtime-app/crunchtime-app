import styled from 'styled-components/native';

const View = styled.View`
    background: #5745ac;
    flex: 1;
	justify-content: space-between;
    align-items: center;
    display: flex;
`;

export default View;