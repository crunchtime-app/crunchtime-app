import styled from 'styled-components/native';
import {colors} from '../../resources';

const Label = styled.Text`
    font-size: 18px;
    /* color: ${(props) => (props.light ? colors.orangeBright : colors.body)}; */
    margin: 5px;
`;

export default Label;
